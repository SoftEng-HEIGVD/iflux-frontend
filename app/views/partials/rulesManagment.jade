.container
    #page-content-wrapper
        h1 iFLUX Rule Managment 
        .col-lg-12
            | Search a rule:
            input(ng-model='search')
            table.table.table-condensed
                thead
                    th
                        a(href='', ng-click="predicate = 'name'; reverse=!reverse") Name
                    th
                        a(href='', ng-click="predicate = 'enabled'; reverse=!reverse") Enable
                    th Modification
                    th Delete
                tbody
                    tr(ng-repeat='rule in rules | orderBy:predicate:reverse | filter:search')
                        td {{rule.name}}
                        td
                            input(type='checkbox', ng-model='rule.active', ng-disabled='true')
                        td
                            a.btn.btn-xs.btn-primary(ng-href='#/ruleEditor/{{rule.id}}') Modification
                        td
                            a.btn.btn-xs.btn-danger(ng-disabled='true', ng-click='delete(rule.id, $index)') Delete
            a.btn.btn-primary(ng-href='#/ruleEditor') Create a new rule
